# ima 云端知识库 RAG 实践（加分项）

> 对应 JD：「用 WorkBuddy + ima 知识库做一次本地知识问答小实践」。
> 本文件是一段**可讲述的经历**，面试时可直接复述；也是本地 BM25 版 RAG 的对照与补充。

---

## 一、做了什么

把《生成式AI短视频内容营销知识库.md》通过 **ima 知识库 MCP 接口**完成「上传 → 解析 → 语义检索」全流程，验证云端 RAG 在概念型查询上的优势，并与此前手写的本地 BM25 版形成对照。

## 二、技术流程（可复述）

1. `get_knowledge_base_list` → 选定个人知识库
2. `create_media` → 拿到 COS 临时上传凭证
3. 用腾讯官方 COS SDK 把文件 PUT 到对象存储
4. `add_knowledge` → 文档入库（后台自动切块 + 向量化）
5. `search_knowledge` → 语义检索问答

> 对比本地版：本地是自己写 BM25 检索；云端是 ima 后台完成「切块+向量化+语义检索」，我只需上传文档、提问。同一份知识，两种检索实现。

## 三、检索结果（与本地 BM25 对照）

| 提问 | ima 语义检索命中 | 本地 BM25 当时 |
|---|---|---|
| 数字人技术对内容生产有什么价值 | 正确召回文档（概念型查询） | ❌ 漏掉 3.3「素材层（数字人）」小节 |
| 短视频投流出价方式 / 赛马放量 | ✅ 命中 4.3 付费投流策略 | ✅ 命中 |
| 完播率多少算优秀，为什么重要 | ✅ 命中 5.1 指标表 | ✅ 命中 |
| 生成式推荐 vs 传统协同过滤 | ✅ 命中文档 | ✅ 命中 |

> 实测证据：文档在 ima 中解析状态 `media_state=2`（解析成功）、`parse_progress=100`；4 类问题均正确召回目标文档/小节。

## 四、核心洞察（最有价值的讲述点）

- **关键词检索 vs 语义检索的取舍**：BM25 按「词面重合」打分，遇到「数字人」这类**概念型查询**且词面不吻合时会漏召回；云端语义检索基于**向量相似度**，能理解概念并正确召回。这正是 RAG 里「关键词 vs 语义」两条检索路线的本质区别。
- **原理派 vs 应用派**：本地版（`rag_qa.py`）证明「懂 RAG 原理、能自己造轮子」；云端 ima 版证明「能用成熟工具、快速落地」。面试时两者讲清，等于既懂底层也会用产品。
- **两段式 RAG 不变**：无论本地还是云端，都是「Retrieval（检索）+ Generation（生成）」两段式；区别只在 Retrieval 用关键词还是语义。

## 五、可直接写进简历的话术

> 将同一份领域知识库分别用「本地 BM25 检索」与「云端 ima 语义检索」两种方案做 RAG 知识问答实践：本地版用 Python 标准库实现检索召回，理解 RAG 原理；云端版通过 ima 知识库 API 完成上传、解析与语义问答，对比发现关键词检索在概念型查询上召回偏弱、语义检索更准，从而厘清 RAG 中关键词与语义检索的取舍。
